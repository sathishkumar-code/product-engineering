---
title: Implementation Note — <slug>
type: implementation-note
slug: <slug>
status: Draft
---

# Implementation Note — <slug>

> Instantiated from `templates/implementation-note-template.md`. Authored by the Developer persona as the handoff artifact from code implementation to QA execution. Save at `<folder>/07_build/{features,enhancements,bugs}/<slug>/implementation-note-<slug>.md`.

| Field | Value |
|---|---|
| Repo | |
| MR / PR | |
| Tracker item | |
| Tech-spec reference | (path to `tech-spec-<slug>.md`, or "N/A — bug fix, see spec.md" etc.) |
| Status | Draft / Ready for QA / Returned |

## What was built

(Plain description of what was implemented, scoped to this story/slug. Reference the acceptance criteria or Business Rule IDs satisfied.)

## Deviations from the tech-spec

(Any point where the implementation differs from the tech-spec or spec.md as written. If none, state "None." A behavior/data-model/API-surface deviation must be routed back to the System Architect per `skill-developer-discipline.md` — note that routing here if it happened.)

## Test coverage mapping

(Which automated tests — unit, integration — cover this change, and which acceptance criteria / Business Rules they map to. This is not QA's execution record — see `qa-execution-report-<slug>.md` for that — this is what the Developer already covered before handoff.)

## Known gaps / follow-ups

(Anything left undone, deferred, or flagged for a later story. If none, state "None.")
