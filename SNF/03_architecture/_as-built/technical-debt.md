# Technical Debt Registry — Shashi Care (Senior Living)

> **Document type:** Technical debt registry (living document — append as debt is discovered)
> **Created:** 2026-06-20
> **Updated:** 2026-07-12 (cross-cutting review cycle — `senior_living_backend` @ `e075f578`, `senior_living_admin` @ `324840a`, `senior_living_staffapp` @ `4aa3849` (`master`), `senior_living_reactnative` @ `3af3c3e`, `senior_living_skillednursing_resident` @ `bd01f4c` (unchanged): added `SL-TD-19` (Critical — unauthenticated `/pcc-sync/*`), `SL-TD-20` (Medium — hotel-demo-slideshow unbounded upload), `SL-TD-21`..`SL-TD-23` (staffapp: dead Reports tab, Terms-acceptance persistence gap, Freshworks hardcoded fallback), `SL-TD-24` (reactnative: shared-cancel-function naming smell), `SL-TD-25`..`SL-TD-30` (skilled-nursing resident app security debt set, promoted from that repo's local §9 — CognitoStorage adapter broken, unencrypted token storage, plaintext remember-me password, unguarded prod logs, inert 7-day HIPAA idle timeout, hardcoded Discord webhook, CI security-scan gap on deployed branches). Prior update 2026-07-03: `SL-TD-18` `DINING_TRAY_TEST_BYPASS`; widened `SL-TD-10`. 2026-06-23: `SL-TD-17` + `SL-DEF-04`. 2026-06-22: `SL-TD-16`. 2026-06-21: staging reconciliation baseline)
> **Owner:** Engineering Lead / System Architect
> **Scope:** Cross-repo Senior Living platform (`senior_living_backend`, `senior_living_admin`, mobile, TV).
>
> **Source of truth:** As of 2026-07-03 this registry is reconciled against the **`production`** branch — `senior_living_backend` @ `465e88fb`, `senior_living_admin` @ `59d22ea`. Prior reconciliation was against `staging` @ `62de4747` / `d2b8d05` (2026-06-21). File:line references were re-verified against the stated baseline; where a fix was "in progress"/"scoped" but did not land, the entry is kept **open** and the status corrected.
>
> **Purpose:** Single registry of known technical debt, security gaps, and "accepted for now" decisions surfaced during UAT acceptance-criteria authoring, architecture review, and implementation. Each entry records the decision taken, why it was accepted, and the condition under which it must be paid down.
>
> **How this grows:** Items are added as they are discovered (e.g. while writing the Redwood Grove UAT acceptance criteria). Entries are not deleted when resolved — set Status to `Resolved` with a date so the history is preserved. Severity follows the platform convention: **Blocker → Critical → High → Medium → Low**.

---

## Registry

| ID | Area / File | Severity | Issue | Decision / Status | Must-fix-by | Source |
|---|---|---|---|---|---|---|
| SL-TD-01 | `senior_living_admin` — Vite build (`VITE_COGNITO_CLIENT_SECRET`) | **Blocker** (security) | The Cognito app-client secret is baked into the JavaScript bundle at build time (admin arch doc Design Gap G1). Any user can extract it from browser DevTools. **Still open on `staging` (re-verified 2026-06-21):** `import.meta.env.VITE_COGNITO_CLIENT_SECRET` is read client-side to compute the Cognito `SECRET_HASH` in `src/utils/helpers.ts:68`, `src/components/LoginPage.tsx:77`, `MFASetup.tsx:25`, and `PasswordChange.tsx:25`. | **Accepted for Redwood Grove first deployment** — does not block UAT execution. Must be acknowledged in the UAT sign-off report and escalated before wider production rollout. Two remediation options documented in the architecture review (move secret server-side / proxy auth). | Before wider production rollout beyond Redwood Grove first deployment | LOGIN-OQ-04 (UAT admin-web login flow) |
| SL-TD-02 | `senior_living_admin` — four auth screens, hardcoded CloudFront CDN logo URL (admin arch doc §6 TD12) | **High** | The sign-in / auth screens display a logo from a hardcoded CDN URL. The facility-specific logo from `Config` is only loaded into `facilityStorage` after login, so a true per-facility logo cannot render pre-authentication. Originally flagged as a single-tenant gap. | **Resolved by design (lead, 2026-06-20).** Product decision: the pre-auth sign-in screen shows the **Shashi Care product logo** (the same for every facility), and the facility logo is shown **after login**. The hardcoded pre-auth URL serving a product logo is therefore correct for all deployments — there is no per-facility pre-auth requirement. The hardcoded URL must serve the current Shashi Care product logo (deployment-checklist DC-06). Entry retained for history. | n/a — closed by design (was: before next property deployment) | LOGIN-OQ-08 (UAT admin-web login flow) |
| SL-TD-03 | `senior_living_admin` — `use-inactivity-logout.ts:18` (`?? 0` fallback) + `senior_living_backend` — `config.model.ts:728` (no schema default) | **Medium** | Inactivity timeout uses a `?? 0` fallback when `Config.inactivityTimeout.web` is missing, which yields an indefinite session (timer never arms); the backend schema also has **no default**. Also tracked as ADMIN-7 in the architecture risk register. | **STILL OPEN — the planned fix did NOT land on staging (re-verified 2026-06-21).** The intended fix (hook fallback `?? 30` and/or schema `default: 30` + a warning log on fallback) was scoped on 2026-06-20 but is **not present on `staging`**: `use-inactivity-logout.ts:18` still reads `config?.inactivityTimeout?.web ?? 0` (indefinite session on missing config), and `config.model.ts:728` still declares `web: { type: Number, min: 1 }` with **no `default`**. Until the fix lands, a missing/failed `inactivityTimeout.web` gives an indefinite session, masked only by deploy-time config (DC-02). Product default remains **30 minutes**. | Open — re-scope the fix; until then masked only by DC-02 | LOGIN-OQ-02 / LOGIN-OQ-07 (UAT admin-web login flow); staging re-verification 2026-06-21 |
| SL-TD-04 | `senior_living_backend` (API authorization) + `senior_living_admin` (page-access UI) | **High** (security) | **RBAC is enforced at the UI level only.** Access is decided by Designation + per-designation page access (Manage Designations, Staff Management) + per-user page access (Access Management page), but these gates are applied **only in the admin-web UI** (showing/hiding pages). The backend API performs **no authorization enforcement** — any authenticated caller (valid Cognito token + `x-facility-id`) can invoke endpoints directly regardless of designation/page-access. Privilege boundaries — including the "admin cannot edit their own Designation" control — are bypassable via direct API calls. **Still open on `staging`; note that a subset of resident routes lack even authentication — see `SL-TD-10`.** | **Accepted for Redwood Grove first deployment (lead, 2026-06-20).** Tracked as debt; does not block UAT (UAT verifies the UI-level behaviour). Backend authorization enforcement is required before wider rollout. **Treat as Critical if the API is reachable outside a trusted network.** | Before wider production rollout / before the API is internet-exposed for multi-tenant use | PROF-OQ-01 (UAT user-profile feature) |
| SL-TD-05 | `senior_living_backend` — profile / user update endpoints | **Medium** (compliance/observability) | **No audit or history trail for profile/user changes.** Changes to profile fields — especially **Email** and **Designation** (which governs access) — are not recorded anywhere with actor / old→new / timestamp. HIPAA posture expects change-and-access auditing of records that affect data access. | **Accepted for Redwood Grove first deployment (lead, 2026-06-20).** Tracked as debt; add an audit/history trail for profile and role/designation changes. Until then, UAT acceptance criterion PROF-AC-10 is documented as a known gap rather than a passing test. | Before HIPAA audit controls are required / wider rollout | PROF-OQ-07 (UAT user-profile feature) |
| SL-TD-06 | `senior_living_backend` — resident / family-member / care-team update endpoints (`resident.controller.ts`) | **High** (compliance/observability) | **No audit or history trail for resident changes.** Resident create/update/delete, **care-team (staff) mapping** changes, family-member changes, and PCC-sourced field updates are not recorded with actor / old→new / timestamp. Residents are **PHI**, so change-and-access auditing carries more weight here than for staff profiles (`SL-TD-05`). Same root cause — no audit mechanism exists. | **Accepted for Redwood Grove first deployment (lead, 2026-06-20).** Tracked as debt; the resident-change audit trail should be built alongside the profile/user trail in `SL-TD-05`. Until then, UAT acceptance criterion RES-AC-10 is documented as a known gap rather than a passing test. | Before HIPAA audit controls are required / wider rollout | RES-OQ-06 (UAT residents-management feature) |
| SL-TD-07 | `senior_living_backend` — resident lifecycle (`resident.controller.ts` create path ~`:406-476` + delete `:1808-1845`; PCC `src/integrations/pms/pcc/webhooks/handlers/patient/readmit.ts`) | **Medium** (product/data lifecycle) | **Resident re-admission must reactivate the existing record — current code does not.** Delete is a soft-delete (`deletedAt = new Date()`, `:1822`) plus best-effort **Cognito user deletion** (`AdminDeleteUserCommand`, `:1834`); `status` is left untouched. On re-add of a same-phone resident, the create path deletes the stale Cognito user (`:406-426`) and **inserts a brand-new document** via `Resident.create([...])` (`:476`, fresh `_id`, same `cName = phone`) — leaving the old soft-deleted row in place and **not** carrying forward prior `status`, care-team, family, or PCC linkage. The PCC `patient.readmit` handler converges `status → Active` (`buildPatientStatus`, `shared/patientData.ts:71`) but **never clears `deletedAt`** (`$set: { ...buildPatientStatus(patientData) }` only), so a PCC readmit on a soft-deleted resident yields a contradictory `deletedAt`-set + `status: Active` ghost. | **STILL OPEN — build pending, NOT yet implemented on `staging` (re-verified 2026-06-21).** Scoped as a build item (lead, 2026-06-21; supersedes the 2026-06-20 deferral) and captured in **ADR-004 (Accepted — build pending)**. Re-admit shall **reactivate the existing soft-deleted record** (match by `{facilityId, countryCode, phone}` / `cName`): **clear `deletedAt`**, set `status` back to `Active` (or per input), and re-create/re-enable the Cognito user — keeping **one** row per phone so prior history/care-team/family/PCC linkage is preserved. Apply to **both** paths: (1) manual re-add (create path — staging still does `Resident.create`, no reactivation), (2) the PCC `readmit` handler (staging still omits the `deletedAt` clear; fix per ADR-001 convergent-state model + CLIN-GAP-18). Also fix the delete dialog "cannot be undone" copy. Manual path is in UAT scope (RES-AC-12); the PCC path is testable only once PCC is configured (DC-08). The shared identity-lookup hazard this exposes is tracked separately as **SL-TD-09**. | Before re-admission ships (manual path = this UAT build; PCC path with DC-08) | RES-OQ-08 (UAT residents-management feature); ADR-004; staging re-verification 2026-06-21 |
| SL-TD-09 | `senior_living_backend` — resident identity lookups (`Resident.findOne({ cName })`, ~15 sites) + `resident.model.ts` indexes | **High** (correctness/data-integrity) | **Resident identity key `cName` equals the phone (E.164) and is not uniquely indexed, so a live + soft-deleted pair can share one `cName` and identity lookups can bind to the dead row.** The `{facilityId, countryCode, phone}` index is **not unique** ("per-facility uniqueness enforced in app" — data-schema §2.1); de-facto uniqueness comes only from Cognito at create time (409 `COGNITO_PHONE_ALREADY_EXISTS`, `resident.controller.ts:690-697`), which does **not** stop a soft-deleted row coexisting with a new active row under the same `cName`. Admin list endpoints filter `deletedAt: null` (so lists look clean and mask the issue), but ~15 per-resident lookups do **not** disambiguate by `deletedAt` — confirmed on `staging` at `tvAuth.controller.ts:215` (`findOne({ cName })`, also no `facilityId`/`deletedAt`); plus `menu.controller.ts:177`, `androidCategories.controller.ts:42`, `config.controller.ts:112`, `pcc.service.ts:35`, `medication.controller.ts:221,323`, `labReports.controller.ts:93`, `chatAccessPolicy.ts:304,336,460`, `rehabMessage/rehabAppointment/rehabAvailability` services — `findOne({ cName })` returns one match arbitrarily (typically the older/first-inserted = the soft-deleted ghost). Login/TV pairing, chat access, medication, and lab-report resolution could bind to the wrong row. | **STILL OPEN on `staging` (re-verified 2026-06-21).** Primary mitigation is **SL-TD-07 reactivation** (one row per `cName` removes the duplicate-row trigger for the manual path) — but SL-TD-07 is itself still build-pending (ADR-004), so neither the trigger nor the lookups are fixed yet. Defense-in-depth still required because the structural gap remains (PCC path, future writers): (a) add `deletedAt: null` to identity `findOne({ cName })` lookups; and/or (b) add a **partial unique index** on `{facilityId, countryCode, phone}` where `deletedAt: null` — precedent: the `Conversation.directPairKey` partial unique index and the `RehabTherapy` code partial index that excludes soft-deleted rows (data-schema §2.1; REHAB-FR-12). Not separately blocked for UAT once SL-TD-07 lands, but must be paid down before duplicate rows can arise by any other path. | Before the PCC readmit path ships / before any second writer can create a same-phone row | RES-OQ-08 investigation (UAT residents-management feature); ADR-004; staging re-verification 2026-06-21 |
| SL-TD-08 | `senior_living_backend` — chat attachments (`src/services/chat/…/attachment.service.ts`, S3 prefix `chat-attachments/{facilityId}/{conversationId}/…`) | **Medium** (security/compliance) | **Chat attachments are not application-encrypted.** Message **text** and the conversation **preview** are KMS-enveloped (AES-256-GCM per-conversation data key — `chatEncryption.service.ts`), but **attachments (images/videos/files, including reports), `mentions[]`, and `reactions[]` are stored unencrypted** at the application layer. Attachments live in S3 and are served via time-limited **presigned GET URLs**; at-rest protection therefore depends on **S3 bucket-level SSE** (SSE-S3 / SSE-KMS), not on app/client-side encryption. Since attachments can carry PHI (the requirement explicitly includes "reports"), this is a HIPAA-posture gap relative to the encrypted text path. | **Accepted for Redwood Grove first deployment (lead, 2026-06-20).** The current implementation (KMS-encrypted text + S3-stored attachments behind presigned URLs, relying on bucket SSE at rest) is **good for this deployment**. Tracked as debt; recommend confirming bucket SSE is enabled. App-level attachment encryption is deferred. **UAT acceptance criterion MSG-AC-13 documents this posture rather than asserting attachment encryption.** | Before HIPAA controls require encryption-in-application for PHI attachments / wider rollout | MSG-OQ-06 (UAT messages feature) |
| SL-TD-10 | `senior_living_backend` — `src/routes/residents.route.ts` (resident CRUD + `/pcc-contacts` + `/check-temp-password` + `/pcc-patient-detail`) | **High** (security) | **Several resident routes have NO `authMiddleware` — they rely only on the global `facilityMiddleware` (`x-facility-id` header) for access.** New on `staging` (re-verified 2026-06-21): `POST /api/residents` (`createResident`), `GET /api/residents` (`getResidents`), `GET /api/residents/:id`, `PUT /api/residents/:id`, `DELETE /api/residents/:id`, `GET /api/residents/check-temp-password`, and the new `GET /api/residents/pcc-contacts` (`getPccContactsForPatient`), and **`GET /api/residents/pcc-patient-detail` (`getPccPatientDetail`) — added on production HEAD `465e88fb`** are all registered without `authMiddleware`. The global gate at `app.ts:104` only validates the facility header, not a Cognito token — so any caller that knows/guesses a facility id can **create, read, update, delete residents and fetch PCC contact PHI unauthenticated**. This is the on-the-ground form of the "backend performs no authorization" posture in `SL-TD-04`, but worse: these endpoints have **no authentication at all**, not merely no role enforcement. (Other routes in the same file — `/profile`, `/pictures`, family-member, etc. — DO carry `authMiddleware`, confirming the omission on resident CRUD is an inconsistency, not a global pattern.) `getPccContactsForPatient` corresponds to admin/backend Design Gap **G-3**. | **NEW — needs decision.** Add `authMiddleware` (and, post-`SL-TD-04`, role enforcement) to resident CRUD, `/pcc-contacts`, and `/check-temp-password`, or document an explicit, justified exemption per route. Treat as **Critical** if the API is reachable outside a trusted network. Until fixed, the same "trusted-network only" caveat as `SL-TD-04` applies, but the blast radius now includes unauthenticated resident PHI read/write. | Before wider rollout / before the API is internet-exposed; sooner than `SL-TD-04` because there is no auth at all on these routes | Staging reconciliation 2026-06-21 (backend G-3); widened production HEAD `465e88fb` (`pcc-patient-detail`) |
| SL-TD-11 | `senior_living_backend` — webhook routes `src/routes/pcc.webhook.routes.ts`, `lemedix.webhook.routes.ts`, `tels.webhook.routes.ts` (mounted `app.ts:214/217/220`, **before** the global facility gate) | **High** (security) | **Inbound integration webhooks have no signature/secret verification.** Re-verified on `staging` (2026-06-21): `POST /webhooks/pcc` routes straight to `handlePccWebhook` with **no signature check** (neither the route nor `pcc.webhook.controller.ts` verifies an HMAC/secret); `POST /webhooks/lemedix` and `POST /webhooks/tels` likewise have no auth (TELS only validates payload **shape** — `authorizationNumber` presence — not authenticity). These handlers mutate resident/PHI state: the PCC handlers (12→14, incl. `patient.admit`, `patient.updateContactInfo`, `patient.readmit`) sync resident + `familyMember` contact data, so a forged request can create/alter resident records (widens the existing PCC forge-able-write surface, backend G-5). The webhooks are mounted **before** `facilityMiddleware`, so they are reachable without even a facility header. | **NEW — needs decision.** Add per-provider authenticity verification (PCC/Lemedix HMAC or shared-secret header; TELS auth) at the route/controller boundary, fail-closed in production. Zoom webhooks already note "Zoom signs payloads" (`app.ts:225-226`) — extend the same discipline to PCC/Lemedix/TELS. | Before PCC/Lemedix/TELS are enabled for a live facility (DC-08 and equivalents) | Staging reconciliation 2026-06-21 (backend G-5) |
| SL-TD-12 | `senior_living_backend` — `src/routes/fax.routes.ts` (`FAX_LOCAL_BYPASS`) | **High** (security) | **The Documo fax routes can run with all auth + facility guards disabled via an env flag, with no production fail-closed.** New on `staging` (Design Gap **G-23**, re-verified 2026-06-21): `fax.routes.ts:16` sets `const bypass = process.env.FAX_LOCAL_BYPASS === 'true'` and, when set, mounts `POST /api/fax/send`, `GET /api/fax/account`, `POST /api/fax/history` with **`guards = []`** (no `facilityMiddleware`, no `authMiddleware`). Faxing is a billable/PHI-bearing action. The flag is documented as "local testing only" and logs a warning, but there is **no `NODE_ENV === 'production'` guard that forces the flag off** — a mis-set env in a deployed environment silently exposes unauthenticated fax send. The fax router is also mounted at `app.ts:103`, **before** the global facility gate, so under bypass it is reachable with no facility header. | **NEW — needs decision.** Make the bypass fail-closed: ignore `FAX_LOCAL_BYPASS` whenever `NODE_ENV === 'production'` (and ideally `preproduction`), or remove the flag and use a test-only fixture. Confirm per-facility Documo credential isolation independently of this flag. | Before the fax feature is enabled for a deployed environment | Staging reconciliation 2026-06-21 (backend G-23) |
| SL-TD-13 | `senior_living_backend` — `src/integrations/documo/documo.service.ts` (`sendFax`, `:198`) | **Medium** (reliability) | **Documo fax send has no retry or idempotency.** New on `staging` (Design Gap **G-24**, re-verified 2026-06-21): `sendFax` issues a single `axios.post(`${baseUrl}/v1/faxes`, …)` with no retry/backoff and no idempotency key. A transient transport failure surfaces straight to the caller (`throwDocumoError`), and a retried client request can double-send a fax. "Soft failures" (2xx + `errorInfo`/`errorCode`) are detected and surfaced but not retried. Delivery is only confirmed asynchronously (status `processing` = queued, confirmed via webhook), so there is no end-to-end send guarantee. | **NEW — accepted for now / track.** Add bounded retry with backoff on transport errors and an idempotency key (or a dedupe window keyed on `{facilityId, faxNumber, payloadHash}`) before GA of the fax feature. Verify per-facility credential isolation on `/account` and `/history` (these accept caller-influenced inputs). | Before fax GA / before high-volume use | Staging reconciliation 2026-06-21 (backend G-24) |
| SL-TD-14 | `senior_living_backend` — `src/scripts/migrateAssignedStaff.ts` (one-time `assignedStaff[]` consolidation) | **Medium** (data-migration/ops) | **The care-team consolidation migration is a manual one-time script, not wired into CI/CD or boot.** New on `staging` (T-14, re-verified 2026-06-21): the resident care-team refactor replaced five legacy per-role fields (`nurse`/`caseManager`/`socialWorker`/`doctor`/`dietitian`) with a single indexed `assignedStaff: string[]` (cName array). `migrateAssignedStaff.ts` backfills the new field, but `grep` finds **no reference to it** in `package.json` scripts, `.gitlab-ci.yml`, `app.ts`, or `server.ts`. If the script is not run by hand against a given environment's data, the notification care-team filter (now driven by `assignedStaff[]`) and the chat mention/care-team gating silently see **empty** care teams — staff stop receiving resident notifications with no error. | **NEW — needs decision.** Wire the migration into a release/deploy step (or an idempotent boot-time guarded migration), and record per-environment run status in the deployment checklist. Until then, add a deploy-checklist item to run it manually before go-live for each facility. | Before each environment cutover to the `assignedStaff[]` model | Staging reconciliation 2026-06-21 (backend T-14) |
| SL-TD-15 | `senior_living_backend` — `src/services/pcc/residentKeyCache.ts` (per-process medication-key cache) | **Low** (correctness/ops) | **New per-process in-memory cache with no active sweep.** New on `staging` (re-verified 2026-06-21): `residentKeyCache.ts` caches unwrapped resident medication data keys (`Map<cName, {key, expiresAt}>`, 30-min TTL) keyed on `cName` (= phone). Unlike the sibling `chatKeyCache` — which now runs an active `setInterval` sweep (see `T-7 Resolved`) — this cache evicts **only lazily on access** (`getCachedResidentKey` checks `expiresAt` on read), so expired key material can sit in memory until next access. It is per-Node-instance, so behaviour differs across replicas, and it shares the `cName` ambiguity called out in `SL-TD-09` (a soft-deleted ghost and a live resident share a key under one `cName`). | **NEW — accepted for now / track.** Low risk (KMS remains source of truth; entries are zeroed on eviction). When `SL-TD-09` is addressed, key the cache on a stable resident id rather than `cName`; consider mirroring the `chatKeyCache` active-sweep pattern so expired key buffers are zeroized proactively. | Opportunistic — fold into the `SL-TD-09` fix | Staging reconciliation 2026-06-21 |
| SL-TD-17 | `senior_living_staffapp` — `src/utils/featureAccess.ts` (`ROLE_GROUP_FLOW_MAP`, `:12-25`) | **Medium** (product/config) | **Staff-app feature access is gated by a hardcoded designation→app-flow map.** `ROLE_GROUP_FLOW_MAP` hardcodes which staff designations route to the MIGRATED app-flow (and therefore see Care Conference, etc.): `Case Manager`, `Doctor`, `Director of Nursing`, `Social Worker` → MIGRATED; `Salon Stylist`, `Transport Driver`, `Housekeeping Staff`, `Activities Director`, `Maintenance Staff` → LEGACY; `Message` → CHAT_HOME. The set of designations that may schedule/record/review a care conference is therefore **baked into the mobile binary**, not driven by facility config / page-access. For Redwood Grove this list is correct, **but it is property-specific** — a different customer will have a different designation set, and changing it today requires an app release. | **NEW — needs decision (lead, 2026-06-23).** Accepted for the **Redwood Grove first deployment** (the hardcoded list matches Redwood Grove). Track to make the mapping **dynamic / customer-configurable** (facility config or per-designation page-access driving app-flow) before a second property with a different designation set onboards. **Generic-product / PRD test cases must not hardcode these role names** — treat care-conference access abstractly (`CC-AC-20`, `CC-OQ-05`). | Before a second property with a different designation set onboards / before the role set must change without an app release | Feature 10 `CC-AC-20` / `CC-OQ-05` (UAT care-conference feature); Decision 6 (lead, 2026-06-23) |
| SL-TD-16 | `senior_living_admin` — `src/components/TransportCalendar.tsx` (no `usePageAccess` gate) | **Medium** (security/RBAC) | **The Transport Calendar component has no UI page-access gate.** Unlike its sibling `AppointmentsTransport.tsx` (`usePageAccess("transport-requests")` at `:50`), `TransportCalendar.tsx` neither imports nor calls `usePageAccess` — it renders and queries transport requests with no front-end permission check (nav id `transport-calendar`, `AppContent.tsx:260`). Today the **backend** calendar path is **ADMIN-only** (`transportationRequest.controller.ts:245-248` blocks STAFF), so the data is server-scoped to admins — but the UI gate is absent, so if the backend guard is relaxed to let staff view the calendar (the open question `TRN-OQ-03`) **without** adding a UI gate, the calendar would be effectively ungated. This is the calendar-surface instance of the UI-only-RBAC posture (`SL-TD-04`); note the prior "Transport Calendar placeholder" gap (a stub view) was separately **resolved** on staging — this is a different, net-new finding. | **NEW — needs decision (ties to `TRN-OQ-03`).** Add a `usePageAccess` gate to `TransportCalendar.tsx` and reconcile it with the intended calendar audience: if Admin-only, the backend guard already enforces it and the UI gate is defence-in-depth; if staff should view it, relax the backend `:245-248` guard **and** add the UI gate together. | Before the calendar audience is widened beyond Admin / before wider rollout | Feature 06 `TRN-OQ-03` (UAT transport feature); staging investigation 2026-06-22 |
| SL-TD-18 | `senior_living_backend` — `src/routes/diningTrayCard.routes.ts` (`menuAuthGate`, `:27-36`) | **High** (security) | **`GET /api/dining-tray/menu` auth is bypassable via an env flag with no production fail-closed.** New on production HEAD `465e88fb`: `menuAuthGate` (`:27-36`) checks `process.env.DINING_TRAY_TEST_BYPASS === 'true'` — when set, it skips `authMiddleware` if a `residentId` query param is present. There is **no production-environment guard** that forces the flag off in a deployed environment. This is the same structural pattern as `SL-TD-12` (`FAX_LOCAL_BYPASS`). The dining-tray menu endpoint returns per-resident daily meal cards, which are considered resident PHI. The two authenticated endpoints (`POST /ingest`, `GET /residents-menu`) are correctly guarded. | **NEW — needs decision.** Follow the same remediation path as `SL-TD-12`: add a production-environment fail-closed guard (or remove the flag and use a test fixture). Until fixed, confirm `DINING_TRAY_TEST_BYPASS` is unset in all deployed environments. | Before the dining-tray feature is enabled for a deployed environment / before GA | Production reconciliation 2026-07-03; `src/routes/diningTrayCard.routes.ts:27-36` |
| SL-TD-19 | `senior_living_backend` — `src/controllers/pccSync.controller.ts` (new), `src/routes/pccSync.routes.ts` (new), mounted `app.use('/pcc-sync', pccSyncRoutes)` at `app.ts:242` | **Critical** (security) | **New unauthenticated, unscoped PCC manual-sync tool writes PHI for an arbitrary `facilityId`.** New on production HEAD `e075f578` (merged 2026-07-10): `POST /pcc-sync/residents` and `POST /pcc-sync/residents/map` are mounted **outside** the `/api` prefix, so neither the global `facilityMiddleware` (`app.ts:124`) nor any `authMiddleware` applies. `facilityId` is read directly from the unauthenticated request body (`pccSync.controller.ts:189-192`) rather than the `x-facility-id` header — so any caller can sync/link PCC residents and KMS-encrypted medications for **any facility it names**, with zero authentication and zero tenant-header requirement. The route file's own comment says it is intended to be "trigger[ed] via Postman/curl" for internal ops use, and separately claims `facilityId` "defaults to `STATIC_INTEGRATION` if omitted" — but the controller code actually 400s when `facilityId` is absent (`:191-192`), so that comment is stale/inaccurate. `STATIC_INTEGRATION` was not found anywhere in the config model on a targeted grep. | **NEW — needs decision.** Add `authMiddleware` + facility-header enforcement (or an explicit S2S `x-api-key` gate) before this ships to any internet-reachable environment; do not rely on "internal network only" as an unstated assumption. If this is genuinely meant to be an ops-only tool, move it behind a documented internal-only network boundary or a dedicated ops auth token, and correct the stale route-file comment. | Before this route is reachable outside a fully trusted network / before wider rollout | 2026-07-12 cross-cutting review (`review-senior_living_backend.md`, Design Gap G-26) |
| SL-TD-20 | `senior_living_backend` — `src/middleware/s3upload.ts` (`buildMulterUnlimited`/`s3UploadUnlimited`, `:+46`), `src/models/hotelDemoSlideshow.model.ts` (new), `src/controllers/hotelDemoSlideshow.controller.ts` (new) | **Medium** (availability/cost) | **Hotel-demo-slideshow uploads have no file-size cap.** New on production HEAD `e075f578` (merged 2026-07-06→07-08): the slideshow feature's uploader (`buildMulterUnlimited`/`s3UploadUnlimited`) has no size limit configured, unlike other upload paths in the same repo. A large or repeated upload can consume S3 storage/egress and backend memory with no bound. Product ownership of this feature is also unclear — branch name references "hotel" (Shashi Hotels), merged via a `pre-production`→`production` cherry-pick into a Senior Living repo; see the open question in `review-senior_living_backend.md` and `review-senior_living_admin.md`. | **NEW — accepted for now / track.** Add a reasonable per-file and per-slideshow size cap before this feature is enabled for any production facility; confirm with product whether this feature belongs in the Senior Living product line at all. | Before the slideshow feature is enabled for a deployed environment | 2026-07-12 cross-cutting review (`review-senior_living_backend.md` Design Gap G-27; `review-senior_living_admin.md` open question) |
| SL-TD-21 | `senior_living_staffapp` — `src/navigation/skilledNursingAppStack/index.tsx:257-283` | **Medium** (product/dead code) | **Reports tab is registered but entirely unreachable.** `<Tab.Screen name={SkilledNursingTabScreens.REPORTS} .../>` is commented out in the MIGRATED tab bar, while the import, route constant, and `ReportsScreen` component all still exist and build. No user (doctor or otherwise) can reach Reports today. Unclear whether this is an intentional interim state (superseded by the new Pending Sign tab for doctor-group users) or an accidental regression. | **NEW — needs product/eng decision.** Confirm whether Reports is intentionally retired, needs re-scoping for non-doctor roles, or should simply be un-commented. Until decided, this is dead code accumulating in the bundle. | Before the next staffapp release that touches the MIGRATED tab bar | 2026-07-12 cross-cutting review (`review-senior_living_staffapp.md` Design Gap G11) |
| SL-TD-22 | `senior_living_staffapp` — `TermsWebViewScreen` (new, added to AuthStack, `a7a2e46`) | **Medium** (compliance-adjacent) | **Terms & Conditions acceptance-persistence semantics are unverified.** A new webview-rendered Terms screen was added to the auth stack this cycle, but whether acceptance is persisted once-per-install, once-per-app-version, or re-required every sign-in was not fully traced. Given this is compliance-adjacent (T&C acceptance), an unverified persistence model is worth closing out explicitly rather than leaving implicit. | **NEW — needs a dedicated follow-up.** Trace the actual persistence key/logic and confirm it matches the intended compliance posture (typically: re-prompt on version bump, not on every sign-in). | Before this is relied on as a compliance control | 2026-07-12 cross-cutting review (`review-senior_living_staffapp.md` Design Gap G12) |
| SL-TD-23 | `senior_living_staffapp` — `src/services/App/FreshworksWidgetService.ts` (new) | **Low** (config hygiene) | **Freshworks customer-support widget has a hardcoded fallback widget ID/URL** if the runtime configuration is absent. This is the first departure from the app's otherwise-consistent "no hardcoded config, everything via runtime configuration" convention (Cognito, Twilio already follow the convention). Low risk today, but worth fixing before the pattern is copied elsewhere. Also newly notable as a third-party integration (loads `widget.freshworks.com`) not previously listed in any platform vendor inventory. | **NEW — accepted for now / track.** Remove the hardcoded fallback; fail closed (hide the widget) when runtime configuration is absent, consistent with the rest of the app's convention. | Opportunistic — before the hardcoded-fallback pattern is reused elsewhere | 2026-07-12 cross-cutting review (`review-senior_living_staffapp.md` TD14) |
| SL-TD-24 | `senior_living_reactnative` — `src/services/health/index.tsx` (`CancelOutsideAgencyService`) | **Low** (naming / maintainability) | **`CancelOutsideAgencyService` is a misleading name — it is the shared cancel function for all three care types** (Physical Therapy, Cognitive Evaluation, and Outside Agency), not an Outside-Agency-only function (`DELETE /api/care/cancel/:id`, called from `PhysicalTherapyListScreen`, `CognitiveEvaluationListScreen`, and `OutsideAgencyServiceListScreen`). Low risk today (the code works), but a future PT/CE-only bug fix could be missed by a reviewer who assumes the function is Outside-Agency-scoped. | **NEW — accepted for now / track.** Rename to something generic (e.g. `CancelCareAppointment`) under its own small ticket. | Opportunistic — low priority | 2026-07-12 cross-cutting review (`review-senior_living_reactnative.md` TD-17) |
| SL-TD-25 | `senior_living_skillednursing_resident` — `services/Auth/cognito.storage.ts:8-17` | **Critical** (auth correctness) | **`CognitoStorage.getItem()` always returns `null`.** Promoted from that repo's own architecture-doc §9 (local-only until now). The function is declared synchronous but wraps an async `AsyncStorage.getItem()` call whose `.then()` resolves after the function has already returned `undefined`/`null`. It works today only because every active auth path bypasses this adapter; any future Cognito-SDK call that relies on it (e.g. `CognitoUserPool.getCurrentUser()`) will silently fail. Same defect class as `SA-2`/`SN-D2` in the platform risk register (`architecture-senior-living-product.md` §5.1/§5.7 — staffapp's equivalent was resolved via a synchronous in-memory cache; this app's was not). | **NEW — promoted for cross-repo visibility.** Rewrite over a synchronous store (e.g. `react-native-mmkv`), or explicitly document the adapter as permanently non-functional and remove the misleading "block until value is read" comment, mirroring the staffapp fix. | Before any future Cognito-SDK-managed storage call is added to this app | Promoted 2026-07-12 from `architecture-senior_living_skillednursing_resident.md` §9 (local debt table); platform register row `SN-D2` (still open for this app) |
| SL-TD-26 | `senior_living_skillednursing_resident` — `services/Auth/token.manager.ts:26-34` | **High** (security) | **Auth tokens (ACCESS/ID/REFRESH) stored in unencrypted AsyncStorage.** Promoted from that repo's own architecture-doc §9. Readable by privileged/rooted-device apps on Android; `react-native-keychain` is already a dependency of this app but unused for token storage. Same defect class as `SA-D2` (staffapp), which has already been resolved there via `react-native-keychain`. | **NEW — promoted for cross-repo visibility.** Migrate to `react-native-keychain` with `ACCESSIBLE.WHEN_UNLOCKED_THIS_DEVICE_ONLY`, mirroring the staffapp fix (`SA-D2`, resolved). | Before wider rollout of this app | Promoted 2026-07-12 from `architecture-senior_living_skillednursing_resident.md` §9; platform register row `SN-D3` |
| SL-TD-27 | `senior_living_skillednursing_resident` — `utils/authStorage.ts:4-9` | **High** (security) | **Remember-me stores the plaintext password in Keychain**, not just the refresh token. The refresh token alone would suffice to restore a session; storing the plaintext password as well is unnecessary exposure. Same defect class as `SA-D3` (staffapp), already resolved there. | **NEW — promoted for cross-repo visibility.** Store only the Cognito refresh token (or phone number) for remember-me prefill; remove the plaintext password, mirroring the staffapp fix (`SA-D3`, resolved). | Before wider rollout of this app | Promoted 2026-07-12 from `architecture-senior_living_skillednursing_resident.md` §9; platform register row `SN-D4` |
| SL-TD-28 | `senior_living_skillednursing_resident` — `cognito.service.ts:49,55,66,71,79,87,126,136,180,199,212,216,230,242`; `SplashScreen/index.tsx:43-85`; `TransportationScreen/index.tsx:437,450` | **High** (PII/security) | **14+ unguarded `console.log` calls run in every production build**, including the username on every sign-in, all auth-state transitions, "Session refreshed successfully", full auth state + token presence on every app launch (`SplashScreen`), and address/lat-lng in `TransportationScreen`. No `__DEV__` guard anywhere in this set. | **NEW — promoted for cross-repo visibility.** Wrap all listed call sites in `if (__DEV__)` or remove; prioritise `cognito.service.ts` (username on every sign-in) and `SplashScreen` (auth state/token presence) first. | Before wider rollout of this app | Promoted 2026-07-12 from `architecture-senior_living_skillednursing_resident.md` §9; platform register row `SN-D5` |
| SL-TD-29 | `senior_living_skillednursing_resident` — `SessionGuard` (HIPAA inactivity auto-logoff), `src/utils/local.constants.ts:11` (`IDLE_TIMEOUT`) | **High** (compliance) | **The HIPAA inactivity auto-logoff control is effectively inert.** `IDLE_TIMEOUT` is set to **7 days**, with realistic (minutes-scale) test values commented out in the same file. A control intended to log a user out after a short idle window instead allows a session to persist for a week — the compliance intent of the control is not met in practice. | **NEW — promoted for cross-repo visibility.** Set `IDLE_TIMEOUT` to a HIPAA-appropriate value (consistent with the platform's other inactivity-timeout controls — see `SL-TD-03`) before this control is relied on for compliance purposes. | Before this control is relied on for HIPAA compliance / before wider rollout | Promoted 2026-07-12 from `architecture-senior_living_skillednursing_resident.md` §9; platform register row "SN idle-timeout" (§5.7) |
| SL-TD-30 | `senior_living_skillednursing_resident` — `android/fastlane/Fastfile:10`; CI config `security-scan` job restricted to `master` only | **Blocker** (security / CI) | **A hardcoded Discord webhook URL with token is committed to source, and the CI security-scan job that would have caught it does not run on the branch actually being deployed.** `Fastfile:10` hardcodes the webhook; the Snyk/Semgrep/Trivy/Gitleaks `security-scan` job runs **only on `master`**, while `production` (the deploy branch) and `staging` trigger full build+sign+deploy **without** ever running the scan. This is the same defect class as platform register row `SN-2` (Blocker) and `SN-D1` (High) — promoted here together because the CI gap is precisely why the hardcoded secret was never caught. | **NEW — promoted for cross-repo visibility.** Move the webhook to a CI-managed secret variable; add `production` and `staging` to the `security-scan` job's branch list (or enforce a verified merge trail from `master` so both promotion branches inherit the scan). | Before the next deploy from `production` or `staging` | Promoted 2026-07-12 from `architecture-senior_living_skillednursing_resident.md` §9; platform register rows `SN-2` (Blocker) + `SN-D1` (High) |

---

## Deferred Scope / Enhancements

> Distinct from the debt registry above: these are **requested behaviours consciously postponed**
> (not gaps in existing code). Each records what was asked, why it was deferred, and the trigger to
> revisit. ID prefix `SL-DEF-NN`. If this table grows large it can be split into its own document.

| ID | Deferred item | Area / repos | Requested by | Why deferred | Revisit / trigger | Decided |
|---|---|---|---|---|---|---|
| SL-DEF-01 | **MFA OTP on authenticated change-password** | `senior_living_admin` (`ChangePassword.tsx`) + `senior_living_backend` (new SMS-OTP endpoint) | PROF-AC-07 / PROF-OQ-05 (UAT user-profile feature) | Net-new build. Cognito `ChangePasswordCommand` cannot raise an MFA challenge, so adding OTP requires a custom **SMS-OTP send/verify** flow plus a **new backend endpoint**. The current direct change-password (current password required; policy matches `LOGIN-AC-09`) is **accepted for the Redwood Grove first deployment**. | Post-first-deployment enhancement; build before any security/HIPAA policy requires step-up auth on credential change. | 2026-06-20 (lead) |
| SL-DEF-02 | **@mention / tagging in direct (1:1) messages** | `senior_living_backend` (`src/services/chat/send.service.ts` mention path) + `senior_living_admin` (`MentionPlugin` already role-agnostic) | MSG-OQ-02 (UAT messages feature) | The backend deliberately populates `message.mentions[]` **only for GROUP conversations** and ignores mentions on DIRECT (`Message.model.ts:129`, `send.service.ts` mention validation). Tagging a resident/staff member therefore works in **groups only**. Direct-message tagging is **not yet scoped** — needs a product+backend decision (notification semantics for a 1:1 mention, whether it differs from a normal direct message). Group tagging (MSG-AC-06 / MSG-AC-07) is **accepted as-is for Redwood Grove UAT**. | When direct-message tagging is scoped as product work; the admin composer already supports `@` role-agnostically, so the change is primarily backend. | 2026-06-20 (lead) |
| SL-DEF-03 | **Resident messaging (resident-app chat)** | `senior_living_skillednursing_resident` (SNF resident app — chat **exists in code**: `MessageScreen`, `ChatSocket`, `/api/chat/care-team-contacts`) + `senior_living_backend` (`chatAccessPolicy.ts` resident gates) | Feature 05 scoping (UAT staff-mobile-messages feature) | **Messaging is staff-only for the Redwood Grove first rollout (lead, 2026-06-21).** The SNF resident app already ships a care-team-gated chat (resident → assigned care-team staff only; no group creation, no search), and the backend `canInitiateChat` resident path is real, but resident messaging is **not in scope for this deployment** (`DC-10` sets `isResidentAllowed=false`). The assisted-living resident app (`senior_living_reactnative`) has **no** chat at all. Not authored as a UAT feature this round. | When resident messaging is turned on for a deployment (`Config.chat.isResidentAllowed=true`): author a resident-mobile-messages feature folder (ACs + scenarios) covering the care-team-gated resident flow and resident↔staff cross-channel. | 2026-06-21 (lead) |
| SL-DEF-04 | **Dedicated audit log for care-conference recording / transcript / summary / share actions** | `senior_living_backend` — `careConference.controller.ts` / `careConference.service.ts` (recording upload, transcript fetch, summary submit, share/unshare) | CC-AC-19 / SC-UAT-0171 (UAT care-conference feature) | **Current field-timestamp attribution is accepted for the Redwood Grove first deployment (lead, 2026-06-23).** Today attribution is via field timestamps (`summaryUpdatedBy`/`summaryUpdatedAt`, per-recording `updatedBy`/`updatedAt`) plus `notificationHistory` — there is **no dedicated audit log** capturing recording upload, transcript fetch, summary submit, or share/unshare with actor / before→after / timestamp. Care-conference content is **PHI**, so a full audit trail carries weight (same family as `SL-TD-05`/`SL-TD-06`). UAT acceptance criterion `CC-AC-19` documents the current behaviour rather than asserting a full audit trail. | Build the care-conference audit trail alongside the profile/resident audit trail (`SL-TD-05`/`SL-TD-06`); before HIPAA audit controls are required / wider rollout. | 2026-06-23 (lead) |

---

## Notes

- **Relationship to deployment checklist:** Some debt items are mitigated at deploy time by a config setting (e.g. SL-TD-03 is masked by DC-02) but the underlying code gap remains and should still be paid down. Closing the deployment-checklist item does **not** close the debt entry.
- **Relationship to per-repo architecture docs:** Detailed gap analysis lives in `architecture-senior_living_admin.md` and `architecture-senior_living_backend.md`. This registry is the consolidated, decision-oriented view.

### Staging reconciliation 2026-06-21 — summary

Reconciled `senior_living_backend` @ `62de4747` and `senior_living_admin` @ `d2b8d05`.

- **Corrected (planned fix did not land):** `SL-TD-03` was marked "being fixed immediately" but the inactivity-timeout fix is **not on staging** (hook still `?? 0`; schema still no `default`) — re-opened.
- **Re-confirmed open:** `SL-TD-01` (Cognito client secret in browser), `SL-TD-04` (UI-only RBAC), `SL-TD-05`/`SL-TD-06` (no audit trail), `SL-TD-07`/`SL-TD-09` (re-admission not implemented; ADR-004 build-pending — create path still `Resident.create`, PCC `readmit` still omits the `deletedAt` clear), `SL-TD-08` (attachments unencrypted).
- **New on staging:** `SL-TD-10` (unauthenticated resident CRUD + `/pcc-contacts` + `/check-temp-password`), `SL-TD-11` (PCC/Lemedix/TELS webhooks unverified), `SL-TD-12` (fax `FAX_LOCAL_BYPASS` no prod fail-closed, G-23), `SL-TD-13` (Documo no retry/idempotency, G-24), `SL-TD-14` (`assignedStaff[]` migration not wired to CI/boot), `SL-TD-15` (process-local resident medication-key cache, no active sweep).
- **Resolved on staging (per-repo arch docs; logged here for cross-reference):** backend **T-3** — duplicate `/api/reports` mount removed (single mount at `app.ts:209`); backend **T-7** — `chatKeyCache` now runs an active sweep `setInterval` that zeroizes expired keys (`src/services/chat/chatKeyCache.ts:33`, `.unref()` at `:45`). admin: the **Transport Calendar placeholder** gap is resolved (real `TransportCalendar.tsx`); the **chatSocket unguarded-logging** gap is downgraded (logs now `import.meta.env.DEV`-guarded) with only `helpers.ts:74` (`console.log({normalizedUsername})`, admin **G5**) still unguarded.
- **TV app (`senior_living_tvapp`):** unchanged between production and staging — no debt delta this cycle.

### Production reconciliation 2026-07-03 — summary

Reconciled `senior_living_backend` @ `465e88fb` and `senior_living_admin` @ `59d22ea` against their production branches.

- **T-15 RESOLVED on production.** `isDevelopmentEnv()` = `env === 'local_development'` only (`src/lib/common.ts:12`). Pre-production no longer triggers the static `'TempP@ssword123'` literal.
- **BE-10 partially mitigated on production.** Default temp password is now a cryptographically random `XXXX-XXXX` (unambiguous charset). The `sendDefaultPassword` flag was re-added to `Config` (Boolean, `default: false`); `Test@123` is issued only when the facility explicitly opts in. The Blocker-class literal can no longer appear by accident in production deployments.
- **SL-TD-10 widened.** `GET /api/residents/pcc-patient-detail` (`getPccPatientDetail`) added on production HEAD without `authMiddleware` — extends the unauthenticated resident-route surface (`src/routes/residents.route.ts`). Entry and area column updated.
- **New: SL-TD-18.** `DINING_TRAY_TEST_BYPASS` env flag can bypass auth on `GET /api/dining-tray/menu` with no production fail-closed. Same structural pattern as `SL-TD-12` (`FAX_LOCAL_BYPASS`); same remediation path applies. (`src/routes/diningTrayCard.routes.ts:27-36`)
- **CareConferenceCalendar live in admin.** `care-conference-calendar` nav route now maps to a real `<CareConferenceCalendar />` component in `senior_living_admin` `AppContent.tsx` (HEAD `59d22ea`). No debt delta.
- **DiningTrayCard + cron-worker.** New 59th Mongoose model and separate container entry point deployed. The Puppeteer/Chromium dependency in the cron-worker means the second container must be sized and monitored separately from the API container.
- **Mobile/TV repos:** unchanged — no debt delta this cycle.

### 2026-07-12 cross-cutting review — summary

Reconciled `senior_living_backend` @ `e075f578`, `senior_living_admin` @ `324840a`, `senior_living_staffapp` @ `4aa3849` (`master`), `senior_living_reactnative` @ `3af3c3e` (`master`), and `senior_living_skillednursing_resident` @ `bd01f4c` (unchanged, doc-verification only). Full per-repo detail in the five review files under `../reviews/2026-07-12/`; product-wide synthesis at `../reviews/2026-07-12/review-senior-living-product.md`.

- **New: SL-TD-19 (Critical).** Unauthenticated, unscoped manual PCC-sync tool (`POST /pcc-sync/residents[/map]`) mounted outside `/api` — accepts an arbitrary `facilityId` in the body, writes PHI. The highest-severity net-new finding this cycle.
- **New: SL-TD-20 (Medium).** Hotel-demo-slideshow upload has no file-size cap; feature's product ownership (Senior Living vs. Shashi Hotels) also unconfirmed.
- **New: SL-TD-21 / SL-TD-22 / SL-TD-23 (staffapp, Medium/Medium/Low).** Dead Reports tab; unverified Terms-acceptance persistence semantics; Freshworks widget hardcoded fallback ID/URL (first departure from the app's environment-config convention).
- **New: SL-TD-24 (reactnative, Low).** `CancelOutsideAgencyService` is a misleadingly named shared cancel function for all three care types (PT/CE/Outside Agency), not Outside-Agency-only.
- **New: SL-TD-25 through SL-TD-30 (skilled-nursing resident app — Critical/High×4/Blocker).** Promoted from that repo's own architecture-doc §9, which had never been cross-referenced into this platform-level registry: broken `CognitoStorage.getItem()` adapter (Critical, mirrors staffapp's now-resolved `SA-2`); unencrypted AsyncStorage token storage; plaintext remember-me password; 14+ unguarded production `console.log` calls including username-on-sign-in; a HIPAA inactivity auto-logoff control set to a 7-day timeout (effectively inert); and a hardcoded Discord webhook secret compounded by a CI security-scan job that does not run on the `production`/`staging` branches actually deployed (Blocker — same defect class as platform register rows `SN-2`/`SN-D1`, now also tracked here for cross-repo visibility).
- **Not new, cross-referenced for completeness:** the chat production-hardening delta (`Message.recipients[]`, group-quorum), the admin-web installable PWA, the care-conference recording encryption + external Lambda writer, the facility-timezone layer, and the digital-signature module are architectural/schema facts documented in `architecture-senior-living-product.md` §2.3/§2.5/§2.7/§2.8 and `data-schema.md` rather than debt-registry rows — they are not defects in themselves, though the `/pcc-sync` tool and the Lambda's undocumented contract are.
- **TV app (`senior_living_tvapp`):** unchanged — no debt delta this cycle.
